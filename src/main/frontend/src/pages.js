import HomePage from './pages/HomePage.vue'
import AboutPage from './pages/AboutPage.vue'

// Page registry: view name (Spring MVC) = page component = client entry name.
export const pages = {
  home: HomePage,
  about: AboutPage
}
