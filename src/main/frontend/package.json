{
  "name": "jsinjava-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client": "vite build -c vite.config.client.mjs",
    "build:ssr": "vite build -c vite.config.ssr.mjs",
    "watch": "vite build -c vite.config.client.mjs --watch"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0"
  }
}
